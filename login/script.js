var attempt = 3; // Variable to count number of attempts.
// Below function Executes on click of login button.
function validate(){
var username = document.getElementById("username").value;
var password = document.getElementById("password").value;
if ( username == "PapaGandhi" && password == "SQHn2UuC34w6uGm6"){

window.location = "encrypt"; // Redirecting to other page.
return false;
}
else{
  if ( username == "Silverdiamond" && password == "HTGXb3HPUD4VNQxS"){

window.location = "encrypt"; // Redirecting to other page.
return false;
}
    
  else if ( username == "Simplykin" && password == "scbpwYwNAgYGVP4g"){

window.location = "encrypt"; // Redirecting to other page.
return false;
    }
  else if ( username == "Chrisisdumbaf" && password == "8FZnU82Bmv2LyEfa"){

window.location = "encrypt"; // Redirecting to other page.
return false;
    }
  else if ( username == "Julian" && password == "ZNFkF2xY5aFR577y"){

window.location = "encrypt"; // Redirecting to other page.
return false;
    }


}
}